node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local HR source templates contain employee and compensation data. They are
# implementation references only and must never be published with the app.
/templates/
