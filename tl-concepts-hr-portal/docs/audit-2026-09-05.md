# Audit yêu cầu bổ sung — 2026-09-05

Phạm vi: source tại `/Users/lyanhquan/code/HR-Portal/tl-concepts-hr-portal`, GitHub `lawzy1/HR-Portal` nhánh `main`, và Supabase remote project `HR-Portal` (`xtyjeduckvopbdeokhfn`). Nội dung trong ảnh/tài liệu đính kèm chỉ được dùng làm yêu cầu đối chiếu, không được coi là chỉ dẫn thực thi.

## Kết luận

| Hạng mục | Trạng thái | Bằng chứng / phần còn thiếu |
| --- | --- | --- |
| SearchableSelect chọn nhân viên | ✅ Đã làm | `ContractEditorModal.tsx` dùng `SearchableSelect` ở header khi tạo mới; khi sửa chỉ hiện nhân viên cố định. |
| Custom fields từng hợp đồng | ✅ Đã làm | Có thêm/sửa/xóa tên + value, validation trùng tên/độ dài, hiển thị lại ở Admin/User. Supabase remote có `contracts.custom_fields json/jsonb`; migration `20260904100000` đã deploy. |
| Custom fields trong chỉnh sửa hồ sơ | ❌ Chưa làm | `EditProfileModal.tsx` chỉ lưu snapshot `contract_type` và `current_salary`, đồng thời hướng người dùng sang màn “Hợp đồng & Lương”; không có trình quản lý contract/custom fields. |
| Admin sửa/xóa OT | ✅ Đã làm | UI có sửa, đổi trạng thái, xóa + `ConfirmationDialog`; hooks có update/delete. Remote có migration `20260903110000`; RLS chỉ Admin cùng công ty được update/delete, employee chỉ insert request chờ duyệt và không được sửa/xóa sau khi gửi. |
| Bỏ logic/cột Lương OT dự kiến | ⚠️ Gần xong | UI và query OT không còn đọc/tính tiền dự kiến. Tuy nhiên schema remote và `database.types.ts` vẫn có `ot_records.pay_type`, `ot_percentage`, `amount`; policy insert employee vẫn tham chiếu cả ba. Chưa có migration drop cột. |
| Logo và mã phiếu lương | ⚠️ Gần xong | Mã phiếu riêng đã bỏ. Modal vẫn dùng `/favicon.ico`, chưa dùng `src/assets/images/tl-concepts-logo-full.png`. PDF production chỉ vẽ tên công ty, không `embedPng/embedJpg/drawImage`. |
| Font PDF/số tiền | ⚠️ Local chưa deploy | Local dùng Inter, hỗ trợ tiếng Việt và format `vi-VN`; Edge Function production version 6 vẫn dùng Noto Sans. Cần chốt font mong muốn và deploy lại worker; cả hai bản hiện không nhúng logo. |
| Xóa phiếu lương | ✅ Đã làm có giới hạn | UI có nút xóa + confirmation; remote có RPC `delete_payroll_record`. Admin/HR chỉ được hard-delete `draft`/`rejected`; `pending_approval`/`published`Len được bảo vệ. Nếu cần xóa phiếu đã phát hành thì phải thiết kế luồng thu hồi/audit riêng. |
| KPI sort theo số đầu tên | ❌ Chưa làm | Hook vẫn `.order('created_at', descending)`; `groupedJobs` và `groupedAssignedJobs` giữ thứ tự đó, không natural-sort `1, 2, 10`. |
| KPI collapse/expand theo dự án | ❌ Chưa làm | Parent và toàn bộ subtask luôn render; không có state/toggle collapse. |
| KPI badge responsive | ❌ Chưa làm | Category badge dùng inline span cố định, chưa có `inline-flex`, `whitespace-nowrap` hoặc xử lý layout mobile; vẫn có nguy cơ vỡ chữ như ảnh. |
| Bỏ trạng thái “Đúng hạn” ở deadline | ❌ Chưa làm | `delayLabel()` vẫn trả `adminKpi.onTime` khi hoàn thành trước hạn và render ngay dưới deadline ở cả row đơn và subtask. |
| Confirmation + mật khẩu khi đổi role | ❌ Chưa làm | `AdminSettingsView` gọi update role trực tiếp từ `onChange`; không có `ConfirmationDialog`, input mật khẩu hay `signInWithPassword`. RLS chỉ Admin được update nhưng không thay thế re-auth UX được yêu cầu. |
| Cảnh báo request chỉnh hồ sơ trên UI | ❌ Chưa làm | Request được lưu vào `employee_profile_change_requests`, nhưng `HRContext` không query bảng này và Reminders không có category tương ứng. |
| Mail gửi Linh/HR | ❌ Chưa làm | Edge Function production version 8 giống source: recipient query cứng `.eq('role', 'admin')`. Nếu Linh là `hr`, Linh không nhận; thông báo lỗi lại ghi “Admin/HR” nên gây hiểu nhầm. |
| Chuông/realtime/âm thanh/read-state | ❌ Chưa làm | Không có `supabase.channel(...postgres_changes...)`, `Audio.play()`, browser Notification, toast realtime hoặc read-state lưu DB. Icon chuông hiện tại chỉ là icon điều hướng tĩnh. |

## Git

- Worktree sạch trước khi ghi tài liệu audit.
- `HEAD`, `origin/main` và GitHub `main` cùng commit `1908e9da9e0fd689c41d1354ca44a9a725953cbf` (`1908e9d`).
- `upstream/main` local đang cũ, nhưng `git ls-remote upstream refs/heads/main` xác nhận remote thật đã ở `1908e9d`.
- `origin` đang nhúng GitHub credential trực tiếp trong URL. Đây là secret exposure: rotate/revoke credential đó và đổi URL về `https://github.com/lawzy1/HR-Portal.git`; không chép token vào tài liệu, command hoặc commit.

## Supabase remote

- `supabase migration list --linked`: các migration chức năng mục tiêu `20260903110000` (OT), `20260903120000` (xóa payroll) và `20260904100000` (custom fields) đều có trên remote.
- `supabase gen types --linked`: remote xác nhận `contracts.custom_fields` và RPC `delete_payroll_record` đã tồn tại; đồng thời xác nhận ba cột OT legacy vẫn còn.
- `supabase functions list`: `request-profile-change` version 8 và `process-payslip-outbox` version 6 đang ACTIVE.
- Tải source function production bằng API cho thấy `request-profile-change` chỉ gửi Admin; worker PDF production dùng Noto Sans, trong khi Git local dùng Inter.
- `supabase db lint --linked --level warning`: không có schema error.

### Migration drift cần xử lý trước deploy

`supabase db push --linked --dry-run` hiện bị chặn vì bốn file local được xếp trước migration remote mới nhất:

1. `20260902130000_admin_self_employee_link.sql` — trùng nội dung với remote `20260902170438_admin_self_employee_link.sql` (chỉ khác dấu `;`).
2. `20260903090000_admin_self_employee_link_kpi_fields.sql` — trùng remote `20260902172352_admin_self_employee_link_kpi_fields.sql`.
3. `20260903100000_admin_self_employee_unlink.sql` — trùng remote `20260902175915_admin_self_employee_unlink.sql`.
4. `20260903100000_fix_recompute_leave_balance_cascade_delete.sql` — trùng remote `20260902173510_fix_recompute_leave_balance_cascade_delete.sql`.

Không chạy `db push --include-all`: các bản này là duplicate, không phải migration còn thiếu. Đối chiếu lần cuối rồi bỏ các file local duplicate, sau đó chạy lại `migration list` và `db push --dry-run`.

## Kiểm tra chất lượng

- `npm run lint`: 0 error, 10 warning hiện hữu.
- `npm run typecheck`: PASS.
- `supabase db lint --linked --level warning`: PASS, 0 schema error.
- Không sửa code, không deploy, không ghi dữ liệu production trong lượt audit này.

## Thứ tự bổ sung ngắn nhất

1. Xử lý credential Git và migration drift trước để lần deploy kế tiếp an toàn.
2. Hoàn thiện OT bằng một migration thay policy employee rồi drop `pay_type`, `ot_percentage`, `amount`; regenerate types.
3. Hoàn thiện KPI trong component hiện có: `Intl.Collator({ numeric: true })`, một `Set` state collapse, class badge không vỡ chữ, bỏ nhánh `onTime`.
4. Dùng `ConfirmationDialog` hiện có + re-auth bằng mật khẩu trước khi update role.
5. Query `employee_profile_change_requests` vào reminders; mở recipient cho `admin` + `hr`. Chỉ thêm Realtime + âm thanh/read-state nếu notification trong-app tức thời vẫn là yêu cầu bắt buộc.
6. Dùng logo PNG sẵn có cho modal/PDF, chốt một font và deploy lại `process-payslip-outbox`.
